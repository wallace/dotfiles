# macOS-only kitty settings. Included from kitty.conf via ${KITTY_OS}.conf,
# last, so anything here overrides the theme.
#
# This is the machine kitty actually runs on today: phoenix is reached over
# ssh from here. Keep genuinely macOS-specific options in this file -- the
# macos_* family, cmd-key mappings, titlebar behaviour -- rather than in
# kitty.conf, so the shared file stays true on both machines.
