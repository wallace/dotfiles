# Linux-only kitty settings. Included from kitty.conf via ${KITTY_OS}.conf,
# last, so anything here overrides the theme.
#
# Empty for now: phoenix has no terminal emulator installed and is reached
# over ssh from the Mac, so kitty has never started with this file. It exists
# so that working directly on phoenix is a matter of putting settings here
# rather than restructuring kitty.conf first.
