# Windows-specific git configuration
# This file is included via includeIf in the main .gitconfig

[core]
	# Use VS Code as editor on Windows
	editor = code --wait
	# Windows line endings
	autocrlf = true

[mergetool "vscode"]
	cmd = code --wait --merge $REMOTE $LOCAL $BASE $MERGED

[merge]
	tool = vscode

[credential "https://github.com"]
	helper =
	helper = !gh auth git-credential

[credential "https://gist.github.com"]
	helper =
	helper = !gh auth git-credential

# 1Password SSH signing (uncomment if using 1Password for SSH)
# [gpg]
# 	format = ssh
# [gpg "ssh"]
# 	program = "C:/Program Files/1Password/app/8/op-ssh-sign.exe"
# [user]
# 	signingkey = ssh-ed25519 AAAA...
